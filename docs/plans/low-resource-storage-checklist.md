# 插件低内存、低负载改造：方案审查与工作清单

原审查基线：`4eea7c586fbc9554012ed4568f839c6d834dd776`；2026-09-10 按 B0=`v2.6.4`、B1=`967915b` 统一修订。
状态：SQLite 账本基础及双驱动原型已实现；2026-09-13 先补齐 schema 3 原子状态、一致性备份和新路径恢复，并在现有 JSONL 运行时接入流式周期快照，见[状态/快照阶段](storage-stage-20260913/README.md)。后续已完成 schema 5 / format 2 磁盘语义投影及组件资源测量，并验证 schema 6 大记录分块前置能力；新增测试、验证说明、日志和产物仅保留本地、不纳入提交。运行时数据库接入、完整语义迁移及整体性能验收尚未完成。现有插件仍使用内存/JSONL，不能把可测试的 SQLite 基础层与实际接管历史混淆。
本清单用于后续实施，不表示数据库后端已经可用或可以立即发布。

统一约束及发布门槛遵循[全面优化总计划](single-container-performance-checklist.tmp.md)的 G01–G08；本专项 P0 对应总 P0，P1 对应总 P4，P2/P3 对应总 P5（依赖总 P1/P2），P4 对应总 P6，P5 对应总 P7。本专项编号不能替代总计划实施顺序。

## 1. 审查结论

“本地数据库保存数据 + 有容量预算的内存缓存 + 增量汇总 + 分批后台任务”方向可行。
它可以保留现有统计、筛选、历史修复、精确计价、导入导出功能，减少常驻内存及全量扫描。
这是统计存储层的较大改造，不是简单更换文件格式；具体性能收益必须实测。

建议优先验证 SQLite。插件内嵌数据库，不要求用户安装数据库服务；驱动选择须通过现有五个平台的共享库构建与实际装载验证。
历史冷查询、重新计价和大导出可能变慢，磁盘 I/O 会增加，不能承诺所有操作同时更快。

不采用以下方式实现本次目标：缩短现有数据保留期限、关闭历史精确计价、删除账本信息、降低导出上限、静默合并不同客户端身份。
冷数据是仍属于现有保留范围、但不常驻内存的数据；超过 `retention_days` 的数据仍按原来的统计淘汰语义处理。
永久归档及任意期限历史查询属于另一个功能，不能借本次改造悄悄改变保留规则。

### 设计起点的代码依据（历史）

下表保留方案制定时的依据；已完成的阶段改动及当前限制以上方进度链接为准。

| 位置 | 现状 | 改造要求 |
| --- | --- | --- |
| `go/accounting.go`、`go/stats.go` | 完整明细和淘汰后的逐请求账本都保存在内存；分组、索引和部分范围查询依赖它们 | 数据、身份索引和高基数汇总一起迁移，不能只搬明细却保留全量内存索引 |
| `go/stats.go`：`writeSnapshot`、`writeStorageSnapshotFile` | 深拷贝完整统计，再整体 JSON 编码写入快照 | 消除日常全量复制；数据库增量事务与备份替代完整快照重写 |
| `go/stats.go`：`configureStorageLocked`、`replayStorageFilesLocked` | 加载快照、重放日期 JSONL 和兼容旧单文件 | 迁移必须处理快照覆盖范围、重复形态、元数据更新及旧聚合残差 |
| `go/stats.go`：`UpsertModelPrice`、`DeleteModelPrice` | 价格更新与统计重算在同一流程完成 | 不能直接变成“返回成功但汇总仍用旧价” |
| `go/dashboard_export_jobs.go`：`captureEventExport` | 一次冻结全部匹配事件与成本，然后分页生成文件 | 用数据库一致性读取或磁盘快照冻结数据，不能退回会漂移的分页 |
| `go/dashboard_export_jobs.go`：下载处理；`go/management.go`：完整统计导出 | 下载使用 `os.ReadFile`；管理响应整体 JSON 编码 | 生成阶段和传输阶段分别优化；旧整包接口仍存在响应大小决定的内存下限 |
| `go/main.go`、`tests/stock-host` | CPA 通过共享库 ABI 传递完整响应缓冲区 | 核实宿主能力；不能假设插件可以直接流式输出 HTTP |
| `go/register.go`、`.github/workflows/build.yml` | 持久化默认关闭；现有 Linux/macOS/Windows 五种目标 | 保留配置语义、插件 ID、路由与 ABI；驱动不得引入未声明的宿主依赖 |

## 2. 功能影响和兼容边界

| 功能 | 必须保持的结果 | 实施约束 |
| --- | --- | --- |
| 原生 usage 统计 | 每个真实请求的数量、token、成功失败、延迟、TTFT 不变 | 接收、提交、查询可见性要明确；不能因为异步队列而静默丢数或让新数据暂时消失 |
| 看板与筛选 | API、模型、来源、凭证、客户端 key、时间范围的结果和联动不变 | 汇总加速仅用于能够精确回答的查询；边界时间和复杂筛选从索引数据补算 |
| 健康网格与趋势 | 桶粒度、时区、成功失败和成本口径不变 | 保留时间戳偏移、合成时间标记及滚动范围语义，不能用每日汇总近似所有查询 |
| 明细与账本 | `max_details_per_model` 的可见明细语义不变；账本继续参与统计 | “缓存中没有”不等于“数据淘汰”；账本不得冒充完整事件出现在列表里 |
| 历史计价 | provider 价格优先级、时段、星期、时区、缓存 token 口径及 models.dev 刷新不变 | 保留逐请求计价依据；同一响应内价格与汇总必须使用相同有效版本 |
| 历史修复、导入和重放 | 现有去重、Claude 缓存修复、旧归因修复、协议关联规则不变 | 缓存修复仍按现有显式开关启用；修复记录与全部派生统计事务更新 |
| 导入导出 | 既有 JSON、JSONL、CSV、gzip、计数、截断、过滤与完整备份语义不变 | 新传输接口可以追加，旧接口继续兼容；不以静默截断实现省内存 |
| 缓存与状态 | ETag/304、失效、错误响应和健康状态保持正确 | 存储故障必须显式可见，不能返回成功的空历史数据 |

### 两个不能隐瞒的限制

1. `storage_enabled: false` 代表不持久化。保留任意数量的逐请求数据、完全不写磁盘、同时保持固定低内存，三者无法兼得。
   保留现有内存模式；数据库模式只用于用户已启用持久化的场景。不开持久化时只能优化缓存和临时分配，不能承诺历史数据规模无关的内存占用。
2. 现有管理 ABI 和整包导入导出无法仅靠内部流式编码获得端到端恒定内存。
   优先追加可校验的分块下载和上传路径，并让内置看板使用它们；旧接口保留兼容及已有容量规则，单独测量其峰值。
   浏览器可能仍需组装最终文件，不能把服务端节省的内存误报为全链路节省。若需改宿主才能实现某种传输方式，必须列为阻塞条件。

## 3. 推荐实现边界

- 磁盘为数据库模式的权威数据源，保存完整明细、精简账本、记录身份、必要旧聚合残差和版本信息。
- 内存仅保存预算内的近期数据、查询结果和待提交数据；对单条大小、字节数、条数、并发数分别计量。
- 对数据库页缓存、连接数、临时排序、映射内存、WAL、任务暂存文件设置并验证资源策略；不能只控制 Go heap。
- 增量汇总避免每次查询扫描全部记录。高基数来源、凭证、客户端分组也存盘，避免其内存随记录增长。
- 单写入协调器批量提交；队列同时受字节数和条数预算约束。持续写入速度超过磁盘能力时，不能同时保证无丢失、无限接收和固定资源；必须测量并报告容量边界，明确背压和故障策略。
- 提交记录、汇总及处理进度同事务完成。查询的可见进度必须一致；如保留待提交覆盖层，它也必须有预算、精确合并且在重启后不产生重复。
- 冷查询按索引读取；分页使用稳定顺序和游标，时间戳相同也不能重复或漏项。
- 导出冻结数据版本与价格版本，优先在短期一致性读事务内分批生成磁盘文件，文件生成后释放事务再下载。
  限制长读事务和导出并发，处理 WAL 膨胀；不能仅冻结时间上界或最大 ID 就认为更新、删除不会影响快照。
- 改价重算使用分批计算和版本化汇总，处理重算期间的新请求、删除、到期、导入和再次改价；完成后原子发布。
  优先保持现有同步接口承诺。若重算无法在现有管理超时内完成，应先验证版本化查询方案；异步任务及 UI 状态属于需要单独确认的交互变化，不能默认纳入“功能不变”。

## 4. 能否继承升级

可以设计为继承现有配置和磁盘数据的升级，目标是不要求修改或重编译 CPA、不更换插件 ID、不要求手动重新导入。
此结论是迁移设计可行，不是迁移已验证通过。发布前必须满足下面的门槛。

- 保持 `storage_path` 解析规则。在原目录旁创建新数据库和迁移状态，原 JSONL、快照、价格文件及稳定 hash salt 保留。
- 识别旧单文件、日期分片、旧版本快照、带 `accounting` 的新快照，以及仅剩聚合但无逐请求信息的数据。
- 按旧恢复语义处理快照与分片覆盖关系。真实记录与仅剩聚合的差额分别导入，避免重复相加；缺失的逐请求时间、身份不能凭空恢复。
- 大快照采用增量解析与数据库暂存，不能先调用旧的全量内存恢复再整体搬进数据库；JSONL 分批迁移。
- 使用来源指纹、事务内迁移游标和明确的记录来源标识保证可重入。不能给实时 usage 套一个全局去重唯一键，误删字段相同但真实发生两次的请求。
- 初次迁移在开始接收 usage 前完成一致性切换。若耗时超过宿主初始化允许范围，先验证有持久化增量日志的在线迁移方案，不能直接丢弃迁移期间的数据或无限排队。
- 迁移校验通过后提交完成标记，再启用数据库。区分未开始、迁移中、已完成及不兼容 schema；禁止每次启动重复导入。
- 中断、磁盘满、权限失败时保留旧数据和可续传状态；不执行旧格式文件清理，不把“数据库为空”当作迁移完成。
- 迁移前原文件可以恢复迁移时刻的数据；升级后新写入的数据不在旧文件里。
  无损降级需要先生成旧版可读的兼容快照及增量并校验，且目标版本应支持相应 `accounting` 格式；仅替换旧插件二进制不构成无损回滚。
- SQLite 启用 WAL 后不能只复制主数据库文件作为备份。采用一致性备份机制，生成清单、校验和及恢复验证；旧备份不在每次启动时自动加载。
- 插件按宿主支持的正常关闭、更新、重新加载流程升级，不承诺无需重启的热替换。

## 5. 实施工作清单

以下按完整大项验收；原型中已覆盖的部分不等于整项完成。任一阶段门槛失败，先修订方案，再推进依赖阶段。

### P0：基准与兼容契约

- [ ] 固定基线提交与测试环境，记录请求接收、统计可见、价格更新返回、导入导出和关闭行为。
- [ ] 准备 10 万、100 万请求数据，包含低/高维度数量、长错误文本、请求头、迟到记录、重复时间戳、账本及旧聚合残差。
- [ ] 测量稳定期及峰值 heap、CPA 进程 RSS、CPU、GC、磁盘写入、启动耗时、请求回调和各查询 p95/p99。
- [ ] 覆盖空闲、持续写入、突发写入、导出、导入、改价、保留清理和多看板并发；把宿主无插件开销单独记录。
- [ ] 确定可接受的内存预算、查询与回调延迟、持久化可靠性和迁移停机预算，形成可复现脚本。

阶段门槛：明确目标机器和负载，能够用同一份数据对照新旧实现；不能用缩短保留时间或减少数据量取得收益。

### P1：驱动与存储原型

2026-09-10 进度：新增 `sqlite_ledger*.go`，覆盖版本化记录/身份字典/迁移游标、批次原子性、读视图、键集索引分页、预算与取消。CGO/纯 Go 候选已做同条件对照，13 项数据库回归通过，关闭路径各做 100 次 race 检查；macOS 两种架构构建和 Linux arm64 原生测试/SDK 装载已有阶段证据。完整 schema（含汇总与残差）、五平台交付和资源验收尚未完成，下面的大项不提前勾选。具体版本、失败复现、工具链安全升级与剩余限制见 [SQLite 实验记录](../../tests/performance/results/20260910-sqlite-foundation/README.md)。

2026-09-13 大记录前置能力：schema 6 新增显式 `ApplyLargeState`，大身份/请求值按 256 KiB 分块，内容、记录、state、进度同事务；旧 `Apply` / `ApplyState` 的 1 MiB 单条契约不变。覆盖共享内容与独立请求 ID、更新/归档/删除回收、冻结读视图、损坏和事务失败、真实 schema 3/4/5 升级及大值备份恢复。修复损坏身份被静默漏读、同名不兼容值扩展被误接受的问题，并削减普通页的重复缓冲。双驱动 race、Linux/macOS arm64 装载通过；普通记录读取分配下降但组件时延仍有增加，大值仍受最大单值支配。超大分组键、全局预算及完整运行时契约继续未完成。所有验证资料仍本地保留；排除测试修正后的实现候选仍有旧 schema 固定断言失败，不宣称提交候选全绿。

- [ ] 比较可嵌入 SQLite 驱动的 Go 1.26 支持、许可证、依赖、共享库符号、二进制大小与额外内存；固定版本。
- [ ] 验证 Linux amd64/arm64、macOS amd64/arm64、Windows amd64 构建，检查宿主是否需要额外动态库。
- [ ] 在支持的宿主环境验证 ABI 装载、usage 回调、正常退出和重复加载，不能仅以交叉编译成功代替运行验证。
- [ ] 验证本地 Docker 挂载目录的权限、锁、WAL 和异常恢复；明确网络文件系统及同路径多实例的支持边界。
- [ ] 建立记录/账本/旧聚合残差/迁移状态/汇总版本 schema；索引覆盖真实筛选与排序，使用查询计划核验。
- [ ] 验证批量事务、限额缓存、读写并发、取消、checkpoint、磁盘满和写入失败行为。
- [ ] 抽象内存与数据库后端边界，保留未启用持久化时的原行为，设计后端选择和已有配置映射。

阶段门槛：实际构建装载通过，原型资源结果支持继续推进；否则换驱动或调整存储方案。

### P2：旧数据迁移与可恢复升级

解析进度（早期检查点）：新增 schema-aware 流式快照/JSONL 解析及旧类型对照，分别保留汇总、明细、账本、provider、时间序列和元数据更新；未知版本、尾随数据和溢出不能进入后续激活。审查同时修复现有恢复路径“无效快照先改实时统计再报错”，并禁止对无效头部启动写入/清理。当时磁盘解析索引、解析游标、重叠协调与残差应用仍待实现，历史证据见 [流式解析检查点](../../tests/performance/results/20260910-sqlite-foundation/validation-stream-parser.md)。

2026-09-10 进度：已增加 schema 2 的旧文件隔离暂存、256 KiB 分块/2 MiB 批次、SHA-256 来源及完整内容核验、原子游标续传和有界读取；超过原型记录上限的旧数据也原样保留。此阶段不会把日志里的元数据更新当作新请求计数。快照/JSONL 语义解析、重叠协调、旧残差、激活及回滚当时仍待实现，不能把 `ready` 暂存标记当作迁移完成。故障回归及源码校验和见 [Go 1.26.6 与迁移记录](../../tests/performance/results/20260910-sqlite-foundation/validation-go1266.md)。

2026-09-13 投影进度：schema 5 / projection format 2 已有磁盘语义索引、原文跨度、带前缀 SHA 的原子续传和 typed 键集分页，保留旧 decoder 的重复字段及数组元素复用规则；修复损坏前缀续传、旧 schema 备份拒绝和伪键集分页。完整双驱动 race、生成/模糊对照、两平台装载及 10 万/百万条三轮组件测量通过对应检查；组件内存下降伴随明显 CPU/耗时/磁盘代价，不等于运行时预算验收。实际残差应用、重叠及 metadata/protocol 协调、权威接管、激活与带新数据回退、孤儿回收和全局预算仍待实现，本节不勾选整体完成。新增测试、验证源码快照和原始日志仅保留本地，不纳入提交。

- [ ] 实现第 4 节的迁移流程、来源检查、游标、完成标记和失败重试。
- [ ] 覆盖有/无快照、旧单文件、快照与分片重叠、损坏行、元数据先到/后到、不同版本缓存 token 语义。
- [ ] 对比总请求、成功失败、各类 token、延迟和成本，以及时间桶、API、模型、provider、来源、凭证、客户端分组。
- [ ] 核对明细数与账本数、可见事件排序、精确导入去重、真实重复请求保留、原文件未被改写。
- [ ] 在迁移各边界模拟终止和重启，验证不重复、不漏记、可以续传且内存不随输入文件整体增长。
- [ ] 演练兼容备份、恢复和带升级后增量数据的降级；记录耗时及额外磁盘需求。

阶段门槛：迁移前后功能结果等价，所有数据差异都有明确旧规则依据；无损回滚路径已实测。

### P3：读写、汇总、修复与计价

- [ ] 将实时记录、元数据补充、导入、重放和修复统一纳入事务，定义查询可见性及故障告警。
- [ ] 用磁盘索引替代全量内存账本及身份索引；近期缓存和高基数分组缓存具有全局预算。
- [ ] 实现精确汇总、范围边界补算、健康网格、来源/凭证/客户端联动，并与基线逐项对照。
- [ ] 保持明细保留上限与统计保留窗口相互独立；到期清理同步修正汇总，增量回收空间。
- [ ] 保持时段、星期、时区、provider、缓存计价及合成时间语义；实现改价一致性方案及并发追赶校验。
- [ ] 移植历史修复与去重，用数据库暂存避免全量构造指纹 map；保持现有修复开关和身份边界。
- [ ] 验证 prices 保存失败、models.dev 刷新失败、数据库不可用、队列满及关闭期间的行为。

阶段门槛：既有功能回归通过，前后端价格与计数一致；不能仅验证全局总量而漏掉筛选后的差异。

### P4：导入导出与后台低负载

- [ ] 分批生成 JSON/JSONL/CSV/gzip 导出，冻结数据和价格，覆盖迟到上报、改价、修复及删除并发。
- [ ] 实现追加的分块传输接口和内置看板兼容路径，验证下载完整性、取消、重试、认证、临时文件生命周期及浏览器占用。
- [ ] 完整统计备份也采用分批生成；大文件导入分块上传并在数据库暂存校验，保留旧导入接口及原子性语义。
- [ ] 旧整包接口单独测峰值，明确其 ABI 限制；不得宣传所有旧端点都已具备固定内存。
- [ ] 限制后台并发与每批工作量；避免长期读事务拖住 WAL 回收，处理磁盘空间不足和超时。
- [ ] 复用已有最近到期定时器，验证数据库提交、检查点、备份、重载与关闭不会引入忙循环或削弱可靠写入。
- [ ] 以增量存储及一致性备份替代周期全量快照，验证不会悄悄削弱异常退出保护。

阶段门槛：大导出生成及新传输路径不再同时持有全部记录，后台任务不会导致写入持续积压。

### P5：验收、文档与发布

- [ ] 运行 Go race/vet、JavaScript、更新脚本、原生 CPA ABI 及五个平台构建测试。
- [ ] 使用 P0 相同数据和负载，按总计划 G03 验证相对完整功能基线 B1 的稳定/峰值插件增量 RSS 至少降低 25%、CPU 至少降低 15%；另报 B0 绝对对照。原稿的 50% 候选值作废，不作收益承诺。
- [ ] 固定维度数量、增加历史记录数时，稳定期内存应由缓存预算主导；再单测高基数维度和大响应，不能混淆两种增长原因。
- [ ] 请求回调、冷热查询、改价、导出和启动按总计划 G02/G06 验证；原稿“允许退化 10%”候选值作废，不能用更慢的交互换取通过。
- [ ] 测量数据库原生内存、OS 页缓存与 CPA 进程 RSS，报告总磁盘占用、写放大和增长趋势；不能只汇报 Go heap。
- [ ] 长时间压测验证内存无持续漂移、队列可回落、WAL/临时文件可回收、数据核对通过。
- [ ] 更新配置字段说明、升级迁移、首次启动耗时、备份恢复、降级及数据库故障排查文档。
- [ ] 先通过明确选择的数据库后端验证，再考虑持久化用户的自动迁移发布；未启用持久化的用户不自动写盘。
- [ ] 记录版本、发布说明、迁移演练及性能报告，全部门槛通过后再提交实现并发布升级。

## 6. 完成定义

满足兼容矩阵、迁移/回滚演练、构建装载验证及性能门槛，才可宣布“功能保持兼容、支持继承升级并降低资源占用”。
当前已推进到可测试的数据库基础层，但没有运行时数据迁移/接管；不能宣称 SQLite 后端已可供用户启用。原型收益、初始化、分块 ABI、工具链升级后的平台重验仍需按清单完成。

## 7. 开发前必须定稿的存储设计

下列为可供原型验证的设计要求，不是当前已经存在的 schema 或配置。

### 7.1 后端接口与数据结构

| 对象 | 必须保存/提供 | 实施检查 |
| --- | --- | --- |
| 记录 | 独立内部 ID、内容版本、真实时间精度、原时区/偏移及合成时间标志、token、身份、状态、关联与元数据 | token/计数保留 int64 语义；不能为了压缩缩为 int32 或丢精度 |
| 明细与账本角色 | 可见完整明细/统计账本角色及生命周期 | `max_details_per_model` 只改变可见明细，保留窗口到期才扣减统计 |
| 身份字典 | 模型、provider、来源、凭证、客户端等标准化身份 | 稳定 hash salt 与现有分组规则不变；高基数字典存盘并按需缓存 |
| 统计与残差 | 精确分段汇总、版本、旧聚合残差 | 旧残差不伪造逐请求时间；模型和 provider 计价依据保留 |
| 迁移/导入状态 | 源文件标识、进度、校验、可见代次及完成标记 | 中断重试可重入，重复文件不导致重计，实时真实重复不误去重 |

- [ ] 接口覆盖追加、元数据补充、按 ID 修复/删除、范围读视图、聚合、导入暂存、备份、到期与关闭；业务层不直接拼接数据库专用逻辑。
- [ ] 查询索引先围绕实际时间/API/模型筛选和稳定排序设计；来源、凭证、客户端组合按查询计划选择，避免过量索引拖慢写入。
- [ ] 时间分段按事件时间而非到达时间计算；迟到数据落回原分段，更新汇总版本和相关缓存。
- [ ] 仅保留所需精确汇总，不把所有维度组合放入内存；精确覆盖不了的查询从索引补算。

### 7.2 写入、可见性与故障

- [ ] 每批分配提交代次；记录、派生汇总和进度同事务提交。持有一致读视图的查询不能混入另一个代次的覆盖层。
- [ ] 写入队列与未提交覆盖层由同一内存所有者持有，提交后释放；回调完成后的即时可见性与旧版一致。
- [ ] 区分接收、事务提交和达到耐久点；不得把提交成功等同于断电不丢数据。明确现有 flush/fsync 配置在 SQLite 中的等价承诺并验证。
- [ ] 队列满、单条超大、磁盘满、忙锁和写入失败分别制定策略。不能同时无限接收、无损且固定资源；超出固定承载负载时反馈压力与可观察故障，不伪造成功。
- [ ] 不默认提高宿主回调时延、依赖宿主重试或新增磁盘暂存语义；选择同步提交/覆盖层方案前验证宿主回调超时和错误传播能力。

### 7.3 驱动与磁盘资源

- [ ] 比较 cgo 与纯 Go 驱动的实测资源、共享库符号冲突、许可证和维护性；现有 c-shared 构建不意味着任何 SQLite 驱动都可安全装载。
- [ ] Linux 分别检查目标镜像 libc/链接方式；Windows/macOS 验证锁、路径、文件替换和关闭，不用交叉编译代替运行检查。
- [ ] 单写入协调器、受限读连接和所有连接合计页缓存；明确 mmap、临时排序、busy timeout、WAL 与 checkpoint 配置。
- [ ] 长查询/导出固定版本时记录 WAL 滞留原因；按磁盘预算控制事务寿命或使用一致性磁盘副本，取消后释放读视图。
- [ ] 检查点不等于备份，不能把现有 snapshot 设置直接重命名为 checkpoint 就认为恢复能力等价；按配置语义分别落实耐久、恢复和备份。

### 7.4 迁移与版本切换状态机

状态为：发现旧数据 → 创建未激活数据库 → 分批导入 → 核对统计/身份/残差 → 持久化完成标记 → 原子激活。任何失败保留原数据和未激活状态；普通启动只打开已激活且兼容的数据库。

- [ ] 对首次启动的时间、峰值内存和额外磁盘空间做预检；初次迁移与常规启动分别报告。
- [ ] 对每个状态以及事务提交前后注入中断，重启后续传或安全回到原状态；不能重复应用元数据更新、修复和汇总。
- [ ] 备份记录 schema、应用版本、价格、salt、数据代次及校验；包含事务一致的数据库内容，不能只复制活动主库文件而遗漏 WAL。
- [ ] 回退前验证目标版本能表示账本；如果不能，明确不支持无损回退到该版本，保留可恢复备份，不悄悄丢弃升级后数据。

交付：后端接口说明、schema/索引说明、配置映射表、状态机故障测试、平台装载结果和总计划 G01–G08 的存储证据。驱动/预算未定、故障策略不明确或迁移核对缺失时，继续原型工作，不进入默认升级发布。
